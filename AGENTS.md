# 智能体使用说明

你是配合 **beautiful-html-templates** 模板库工作的智能体。你的任务是：拿到用户的需求简述，**选定合适的模板 → 克隆它 → 用用户的真实内容替换占位内容**，产出一份完成的 HTML 演示文稿。

这份文档是你的操作手册。任何演示文稿任务开始前先通读一遍。

---

## 1. 完整流程

对每一个演示文稿需求，严格按下面顺序执行。**不要**跳过澄清环节和预览环节。

### 第 1 步 — 先问用户场合和情绪

读任何文件之前，先问用户：

> "选模板前先问两个问题：
> 1. **是什么场合？**（比如创始路演、研究综述、品牌宣言、课堂开场等）
> 2. **想要什么情绪／氛围？**（比如自信有力、安静文学、温暖俏皮、暗黑情绪等）"

等用户回答，先别急着选。哪怕需求看起来很明显也问——用户的*品味*常常藏在只靠需求推断猜不到的地方。

### 第 2 步 — 读 `index.json`，选出 3 个候选

读仓库根目录的 `index.json`。把用户说的场合 + 情绪，和每个模板的 `mood`、`tone`、`best_for`、`formality` 对一遍。**选出三个气质真正对得上的模板**。这三个要*彼此够不同*，让用户有真实选择——比如需求是编辑风，就别选三个编辑风；选一个编辑风、一个更温暖的替代、一个重新解读需求的外卡。

### 第 3 步 — 给每个候选做封面页预览

对每个候选：

1. 读它的 `template.html`，看懂它的视觉系统。
2. 只取**第一页**（该模板的封面／标题页）。
3. 把占位内容换成**用户真实的演示主题／标题／副标题／作者／日期**——也就是让预览是真实的，不是泛泛的。
4. 把预览存成临时目录里的独立 HTML 文件，例如 `previews/01-<slug>.html`。保留模板需要的同目录资源（`styles.css`、`deck-stage.js` 等），保证预览能正常打开。

这三个预览文件应当是自包含的——打开任何一个，都能看到该模板的封面页，且填的是用户的真实内容。

### 第 4 步 — 在浏览器里打开 3 个预览，把路径发给用户

用 `open <path>`（macOS）逐个打开 3 个预览文件。然后发消息给用户，例如：

> "三个候选，对照看看：
>
> 1. **<模板 A>** — <一句气质描述>
>    `/path/to/previews/01-template-a.html`
> 2. **<模板 B>** — <一句气质描述>
>    `/path/to/previews/02-template-b.html`
> 3. **<模板 C>** — <一句气质描述>
>    `/path/to/previews/03-template-c.html`
>
> 哪个感觉对？"

等用户选。

### 第 5 步 — 在选定模板里做完整个演示

用户选定后：

1. 把选定模板的整个文件夹克隆进用户的项目工作区。
2. 按第 3 节的规则（保留 / 替换 / 扩展）改写每一页。
3. 用户内容比模板演示页多，就复制已有的版式来填；少了就从底部删页。记得更新页码标注。
4. **如果某一页需要模板没有的版式，就用模板的设计系统从头设计**——同样的字体、同样的配色、同样的装饰语汇、同样的间距节奏、同样的组件语法。不要甩回给用户；不要换别的模板；不要引入新的视觉语言。新页要像模板的自然延伸，而不是嫁接上去的。（见第 5 节——设计缺失的版式。）

### 第 6 步 — 在浏览器里打开最终演示，发送文件路径

用 `open <path>` 打开成品。发消息给用户，例如：

> "好了。你的演示在 `/path/to/deck/template.html` —— 已经在浏览器里打开。
>
> [一句话说明你做了什么，以及要注意的坑。]"

这适用于**你产出的每一个产物**——预览文件、中间迭代、最终演示。永远打开它，永远发送路径。

---

## 2. index.json 里有什么

```jsonc
{
  "schema_version": 1,
  "template_count": 35,
  "templates": [
    {
      "slug": "neo-grid-bold",
      "name": "Neo-Grid Bold",
      "tagline": "米白纸上的编辑感新粗野主义，一抹霓虹黄点缀。",
      "mood": ["自信", "有力", "编辑感", "现代"],
      "occasion": ["产品发布", "设计评审", "创始人路演", ...],
      "tone": ["大胆", "极简", "设计驱动", "图形化"],
      "formality": "medium",
      "density": "high",
      "scheme": "light",
      "best_for": "适合一切该自信、编辑感图形化的内容 ...",
      "avoid_for": "需要安静、传统或温暖的场合不适合 ...",
      "slide_count": 13
    },
    ...
  ]
}
```

字段说明：

| 字段 | 用法 |
|---|---|
| `mood` | 情绪形容词。和用户的*感受*关键词比对。 |
| `occasion` | 示例用例。当软信号用，别当硬过滤。 |
| `tone` | 语气／气质。比对"俏皮""严肃""文学"这类描述。 |
| `formality` | `low` / `medium-low` / `medium` / `medium-high` / `high`。对照用户的受众做个合理性检查。 |
| `density` | 每页能承载多少内容。对照用户的内容量。 |
| `scheme` | `light` / `dark` / `mixed`。用户明确要浅色或深色时，这是硬信号。 |
| `best_for` | **感觉** + 示例场景。介绍你的选择时从这里说起。 |
| `avoid_for` | 气质*冲突* — 软警告，不是否决。 |
| `slide_count` | 演示页数量。提示这个模板演示了多少版式。 |

只有当你把某个模板列入候选后，才需要更深入地读它的 HTML/CSS。

---

## 3. 怎样改写一个选定模板

把模板文件夹克隆进用户工作区后，遵守这些规则。

### 一律保留（这些就是设计系统）

- **字体。** 模板从 Google Fonts 引入的、或在 `font-family` 里声明的——都保留，绝不替换。
- **配色。** 所有 `:root` CSS 变量和色值。绝不重配色。
- **布局网格。** 网格列、绝对定位、flex 层级。别重构。
- **页面级 CSS 类**（如 `.s-toc`、`.slide--quote`、`.layout-cover`）。它们承载视觉身份。
- **装饰元素**——四角括号、纸张纹理、几何形状、插画 SVG、手绘涂鸦。它们是系统的一部分，不是可选的点缀。
- **导航运行时**——不管是 `deck-stage.js`、内联键盘处理、scroll-snap 还是别的。模板用什么就继续用什么。

### 一律替换（这是用户的内容）

- **标题** — `<h1>`、`<h2>`、`<h3>` 等。
- **正文** — `<p>`、列表项、图注。
- **数字和统计** — `47%`、`2.4M`、`+142%` 这类占位值。
- **名字、日期、署名** — 作者名、引文行、"May 2026" 这类占位。
- **章节标签和边角文字** — 模板用的 `[Topic]`、`[Year]`、`[Studio]` 等占位符。
- **图片占位** — 把 `<div class="img-placeholder">Image Placeholder</div>` 或 `<img src="placeholder.jpg">` 换成用户的真实图片，**保持相同尺寸**。

### 加更多页（已有版式）

用户内容比模板演示页多，就复制最合适版式的一页并替换内容。更新页码标注（`NN / TT`）。

### 删页

用户不需要的页从底部删。更新页码标注。

---

## 4. 气质优先匹配

模板有**气质**，不是行业。`best_for` 和 `avoid_for` 描述的是模板给人什么*感觉*，不是它属于哪个行业。

意思是：**一份自信的编辑风演示也能讲技术分享**，只要用户就想显得设计驱动。**一份俏皮粉彩演示也能做金融评审**，只要用户是有意拒绝正式金融的观感。用户的品味说了算。

匹配时：

- **从 `mood` + `tone` + `best_for` 入手。** 对用户想要的*感觉*。
- **把 `avoid_for` 当软警告，别当硬规则。** 用户明确就要模板 `avoid_for` 里提醒的那种观感时，用户说了算。
- **用 `formality` 和 `density` 做合理性检查** — 一份低正式度的模板拿去给董事会做高级汇报，不管气质多贴合基本都还是错的，这点要提醒用户。
- **别过度套 `occasion`。** 那是示例场景，不是金科玉律。
- **第 1 步问的是*气质*，不是*行业*。** 好问题："要显得精致有权威感，还是温暖设计驱动？" 坏问题："是金融还是科技？"

---

## 5. 设计缺失的版式（扩展一个模板）

有些需求需要选定模板没有的版式——比如模板只有流程图和统计网格，你却需要对比表。这时**用模板现有的设计系统来设计缺失的那页**。别逃避，别换模板，别引入新视觉语言。

规则：

- **同样的字体。** 用模板给 h1 / h2 / 正文 / mono 用的相同 `font-family` 声明。同样的字重、字距、行高。
- **同样的配色。** 用已有的 `:root` CSS 变量。新版式需要"警示"或"高亮"色而调色板里没有时，选最接近的现有强调色，别引入新 hex。
- **同样的装饰语汇。** 模板用四角括号、纸张纹理覆盖、手绘涂鸦、几何形状等——新页也用同样的语汇。一张在满是装饰的模板里毫无装饰的裸页会显得坏掉了。
- **同样的间距节奏。** 模板页面用 `padding: 64px`，新页也一样。用 8 列网格配 24px 间距，你也照做。
- **同样的组件语法。** 模板的统计卡用特定结构（大数字 → 标签 → 描述 → mono 图注），新统计类元素就复用这套结构，别发明别的。
- **同样的边角。** 顶部标签 / 底部页码 / 角落标记——和其余页面保持一致。
- **同样的导航行为。** 新页要接入模板现有的导航（deck-stage / 内联键盘 / scroll-snap）。按现有页面的结构，加成一个同级 section / div。

一个好的检验：把新页放在已有两页之间打开。如果它明显*属于这里*——同样的字体、颜色、装饰、节奏——就成了。如果看着像别的模板嫁接上来的，就失败了，重做。

---

## 6. 常见坑

- **别跳过第 1 步。** 哪怕需求很详细，也明确问场合和情绪。用户亲口说的*品味*几乎总能揭示需求里没有的东西。
- **别跳过第 4 步的预览。** 封面页是最快、最便宜让用户感受哪个模板对的方式。别用文字跟他们讲利弊；给他们看。
- **别替换字体。** "Inter 差不多" —— 不，排版就是设计系统。原来的 Cormorant Garamond 没加载出来，就修 import，别换字体家族。
- **别重配色。** 哪怕一点点强调色偏移都会破坏配色的和谐。
- **别把不同模板的版式拼在一起。** 每个模板是封闭的视觉系统。把 raw-grid 的 A 页和 neo-grid-bold 的 B 页混在一起会很业余。（这和第 5 节不同：扩展一个模板没问题，拼两个模板不行。）
- **别把"多余"装饰当成噪声剥掉。** 四角括号、纸张纹理、SVG 装饰——它们是身份的一部分。
- **别试图"现代化"旧模板** — 它们就是按设计在工作的。模板看着过时，就换一个模板，别改现有的。
- **别忘了在浏览器里打开文件。** 每个预览、每次迭代、每个成品。用 `open <path>`。

---

## 7. 输出契约

每产出一个产物——封面预览、中间迭代、最终演示——都要做这两件事：

1. **在浏览器里打开文件。** 用 `open /绝对路径/文件.html`。别只是嘴上说，真的打开。
2. **把绝对文件路径发给用户**，单独一行，格式化成可点击的路径。

最终演示还要加上：
- 一行说明：选了哪个模板、为什么（*气质匹配*）。
- 任何要注意的坑（比如"第 4、7 页你需要的对比表和四列时间线，演示版里没有，我用模板设计系统从头设计了"）。

别一步步叙述你做了什么。用户要的是产物 + 路径 + 一行理由，不是流水账。